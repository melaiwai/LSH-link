from setuptools import setup

setup(name = "lsh",
      version = "1.0",
      author='Melanie Lai Wai and Weisong Zhu',
      author_email='',
      url='',
      py_modules = ['LSH-link'],
      packages=setuptools.find_packages(),
      scripts = ['run_sta663.py'],
      python_requires='>=3',
      )
